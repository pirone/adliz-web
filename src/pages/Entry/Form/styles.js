import styled from 'styled-components';

import { Button } from 'react-bootstrap';

import { Combobox } from '../../../components/Form';

export const CustomCombo = styled(Combobox)`
  width: 95%;
`;

export const BtRemoveItem = styled(Button)`
  padding: 0px 4px;
`;
